rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permisos completos para entorno local y de desarrollo aislados en el emulador
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
